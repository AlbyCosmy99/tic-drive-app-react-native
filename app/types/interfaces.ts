export interface SegmentedControlSelection {
    index: number, 
    name: string, 
    placeholder: string, 
    inputLabel: string
}

export interface VehicleRegistrationOption {
    index: number;
    name: string;
    placeholder: string;
    inputLabel: string;
    keyString: string
}